<%= render 'layouts/errors', :object => @donation %>

<%= bootstrap_form_for [@scholarship, @donation] do |f| %>
  <%= f.number_field :amount %>
  <br>
  <%= f.submit "Submit" , :class => "btn btnWhite"%>
<% end %>
